name: docker-composer
description: Docker Compose file generation and optimization
version: 1.0.0
status: draft
tier: tactical
category: documentation
requires_opus: false
triggers:
  keywords:
  - docker compose
  - compose file
  - docker-compose.yml
runtime:
  mode: preload
  discovery: claude-native
  execution:
    preferred: claude-native
    fallbacks:
    - gemini-cli
    - mcp
